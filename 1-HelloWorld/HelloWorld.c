#include<stdio.h>

/**
 * The default return type of main is int in C.
 * C++ compilers give a compilation error if return type not explicitly specified for main.
 */
main() {
    printf("Hello, World\n");
}
